// Fill in your own Supabase project's values (Project Settings > API in the
// Supabase dashboard). The anon/public key is DESIGNED to be exposed in
// client-side code like this — access to data is controlled by the Row Level
// Security policies on the `scenarios` table, not by keeping this key secret.
// Never put the `service_role` key here; that one must stay server-side only.
//
// Until both values below are filled in, account features stay hidden and
// the rest of the calculator works exactly as it does without an account.
const SUPABASE_URL = '';
const SUPABASE_ANON_KEY = '';
